.terraform/
# .terraform.lock.hcl IS committed on purpose — it pins provider versions and
# hashes, so a clone reproduces the same plan. Reproducibility is the point.
*.tfstate
*.tfstate.*
*.tfvars
!example.tfvars
tfplan
*.tfplan
crash.log
.infracost/
.env
*.pem
